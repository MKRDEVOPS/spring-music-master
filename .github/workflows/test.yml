name: First WorkFlow Ex
on: push
jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a String
        run: echo "Hello World"
      - name: Multiline Script
        run: |
          node -v
          npm -v

